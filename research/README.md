# Research / off-main-path workspaces
# 研究区 / 未入主路径的工作区

| Workspace | Path | DESIGN | Status |
|-----------|------|--------|--------|
| ln(T+T) optimization | [`ln_t_plus_t.py`](ln_t_plus_t.py) | §1.8 | **Incomplete** — not wired into `symbolic/pipeline.simplify_expr` |

主路径：`codec/` + `tree/` + `symbolic/` + `primitives/` + `compile/`。  
Main path: `codec/` + `tree/` + `symbolic/` + `primitives/` + `compile/`.
